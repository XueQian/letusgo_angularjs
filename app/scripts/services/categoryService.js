//'use strict';
//
//angular.module('angularLeteusgoApp')
//  .service('categoryService', function () {
//
//    this.printCategory = function (item, cartItems) {
//
//      var hasExistItem = _.any(cartItems, function (cartItem) {
//        return item.category === cartItem.item.category;
//      });
//
////      if (hasExistItem) {
////
////        var existCartItem = _.find(cartItems, function (cartItem) {
////          return item.category === cartItem.item.category;
////        });
////        //existCartItem.count++;
////
////      } else {
//      if(hasExistItem){   cartItems.push({item: item, count: 1});}
//
//     // }
//
//      return cartItems;
//    };
//  });
